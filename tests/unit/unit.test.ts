import { describe, it, expect } from "vitest";

describe("example unit test", () => {
  it("sanity check — replace with real unit tests as components are built", () => {
    expect(1 + 1).toBe(2);
  });
});