{
  "name": "@build-me/contract-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test:contract": "start-server-and-test \"pnpm --filter @about-me/api dev\" http://localhost:4000/health \"newman run collections/BuildMe-API.postman_collection.json -e environments/local.postman_environment.json\"",
    "test:contract:staging": "newman run collections/BuildMe-API.postman_collection.json -e environments/staging.postman_environment.json",
    "test:contract:production": "newman run collections/BuildMe-API.postman_collection.json -e environments/production.postman_environment.json"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "start-server-and-test": "^3.0.11"
  }
}
