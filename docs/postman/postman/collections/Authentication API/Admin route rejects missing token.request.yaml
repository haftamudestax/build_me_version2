$kind: http-request
url: "{{baseUrl}}/api/leads"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status code is 401 without a token', function () {
        pm.response.to.have.status(401);
      });
    language: text/javascript
order: 2000
